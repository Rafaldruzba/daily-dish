# Skopiuj do frontend/.env.local, jeśli API działa pod innym adresem

# Adres API CRM używany przez serwerowe pobieranie danych
# (w przeglądarce żądania idą przez rewrite /api/* z next.config.ts)
API_URL=http://localhost:3002/api
